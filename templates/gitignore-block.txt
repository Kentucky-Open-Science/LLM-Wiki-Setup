# === sync policy: data and artifacts never reach the remote ===
# (managed block — source: <wiki>/config/; a pre-commit hook additionally
#  blocks any file over the size limit regardless of name)
data/
datasets/
outputs/
output/
results/
checkpoints/
ckpts/
runs/
logs/
*.env
.env*
!.env.example
__pycache__/
.venv/
node_modules/
.DS_Store
Thumbs.db
# --- add the formats of YOUR bulk artifacts below (generator: from the
# --- interview — e.g. *.pt *.ckpt *.safetensors for ML; *.parquet *.h5 for
# --- data work; build/ dist/ for products; *.pdf only if generated)
